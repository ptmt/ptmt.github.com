---
title: Garage
description: Be careful, a lot of stuff lying around
---
{{#content "header-title-suffix"}}
<h1 class="site-title-context">
    <span class="site-title-separator">/</span>
    <span class="site-title-context-text">garage</span>
</h1>
{{/content}}
<div class="garage-page">
    <div class="garage-grid">
        {{#with datasource}}
        {{#each entities.garage}}
            {{#unless metadata.hidden}}
            <article class="garage-card">
                <a href="{{source.path}}" class="garage-card-link">
                    {{#if metadata.image}}
                    <div class="garage-card-image">
                        <img src="{{metadata.image}}" alt="{{title}}" />
                    </div>
                    {{/if}}
                    <div class="garage-card-content">
                        <h3 class="garage-card-title">{{title}}</h3>
                        {{#if metadata.summary}}
                        <p class="garage-card-summary">{{metadata.summary}}</p>
                        {{/if}}
                        {{#if metadata.tags}}
                        <div class="garage-card-tags">
                            <span class="tag">{{metadata.tags}}</span>
                        </div>
                        {{/if}}
                    </div>
                </a>
            </article>
            {{/unless}}
        {{/each}}
        {{/with}}
    </div>
</div>

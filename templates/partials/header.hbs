<header>
    <div class="header-top">
        <a href="/" class="site-title">
            <span class="logo-shape"></span>
            {{siteName}}
        </a>
        {{#unless posts}}
        <nav class="main-nav">
            <a href="/posts">Blog</a>
            <a href="/garage">Garage</a>
            <a href="/about">About</a>
        </nav>
        {{/unless}}
    </div>
    {{#if posts}}
    <div class="header-bottom">
        <p class="site-description">
            journal of work & experiments
        </p>
    </div>
    {{/if}}
    {{#if post}}
    <div class="header-bottom">
        <p class="site-description">
            journal of work & experiments
        </p>
    </div>
    {{/if}}
    {{#if page}}
    <div class="header-bottom">
        <p class="site-description">
            journal of work & experiments
        </p>
    </div>
    {{/if}}
</header>
